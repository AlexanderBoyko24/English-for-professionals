/* ================================================================
   НАСТРОЙКИ ПОДКЛЮЧЕНИЯ
   ----------------------------------------------------------------
   Пока оба поля пустые, приложение работает в локальном режиме:
   аккаунты и прогресс хранятся только в этом браузере.

   Чтобы включить настоящую авторизацию и общую базу, вставьте сюда
   два значения из вашего проекта Supabase (Project Settings → API).
   Подробная инструкция — в файле SETUP.md.
   ================================================================ */

window.APP_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: ''
};
